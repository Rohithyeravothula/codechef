#include<stdio.h>
main()
{
    int t;
    scanf("%d",&t);
    while(t--)
    {
        int n;
        scanf("%d",&n);
        for(i=0;i<n;i++)
        {
            s=0;
            for(j=0;j<n;j++)
            {
                scanf("%d",&m);
                s=s+m;
            }
            if(s%2!=0 && cb==-1)
                {p=-1;
                break;}
            if(s%2!=0 && cb==0)
            {
                cb=-1;
                cn=i;
            }
        }
        for(i=0;i<n;i++)
        {
            s=0;
            for(j=0;j<n;j++)
                s=s+a[j][i];
            if(s%2!=0 && rb==-1)
                {p=-1;
                break;}
            if(s%2!=0 && rb==0)
            {
                rb=-1;
                rn=i;
            }

        }
        if(p==-1)
            printf("0\n");
        else
            printf("")

    }
}
